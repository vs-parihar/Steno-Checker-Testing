const nrm=s=>s?s.normalize('NFC').replace(/[\u200B-\u200D\uFEFF]/g,'').trim():'';
const isW=t=>/[a-zA-Z0-9\u0900-\u097F]/.test(t);
const isS=t=>/^\s+('tx').value,w=t.trim().split(/\s+/).length;('st').textContent='W: '+w;if(f||w>0)sync('w');updateStatus()}
function cropTxt(pos,cnt){if(!origTxt)origTxt=('tx').value=origTxt;return}let sl;if(pos===0)sl=t.slice(0,cnt+20);else if(pos==='mid'){const m=Math.floor(t.length/2);sl=t.slice(m-(cnt/2),m+(cnt/2)+20)}else sl=t.slice(t.length-cnt-20);$('tx').value=sl.join(' ')+' ...';upWC(true)}
